// This is the ternary operator , is the reduing the line.
// Program 1
var isGoing = true;
var color;

if (isGoing) {
  color = "green";
} else {
  color = "red";
}

// Program 2

var isGoing = true;
var color = isGoing ? "green" : "red";
console.log(color);