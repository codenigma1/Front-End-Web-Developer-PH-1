// the beautiful notion of checking prime number..........
// Awesome program.

function is_prime(integer) {
 	for (var i = 2; i < integer; i++) {
 		if (integer % i === 0) {
 			return "The number is divisible by " + i;
 			return false;
 		}
 			
 		}
 		return true;		
 	} 